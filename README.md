# Requirements App

Django web app to manage requirements across projects.

## Development

Here are some useful commands to manage the application:

```bash
uv run manage.py runserver # start the development server
```